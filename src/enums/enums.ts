export namespace Enums {
    
    export enum ParkingType {
        Unknown = 0,
        Parking = 1
    }

    export enum ParkingStatus {
        Unknown = 0,
        Open = 1,
        Full = 2, 
        Unavailable = 3,
        Closed = 4
    }

    export enum ParkingArea {
        Unknown = 0,
        Red = 1,
        Orange = 2,
        Green = 3,
    }
}